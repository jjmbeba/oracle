# Domain JSON and GeoJSON Map Endpoints

Oracle will expose domain-shaped JSON for product views and GeoJSON-shaped endpoints for map layers. MapLibre benefits from GeoJSON, but region dossiers, feeds, watched regions, and change reports need responses shaped around Oracle's domain concepts.


# Hono Routes as API Contract Source

Oracle will treat Hono route definitions as the source of truth for the API contract and generate OpenAPI/client artifacts from them where useful. This keeps backend development ergonomic while still giving the separate Vue frontend an explicit contract to consume.


# MapLibre as Map Engine

Oracle will use MapLibre GL directly through a small local Vue wrapper rather than depending on a higher-level Vue map abstraction. The map is the primary workspace, and direct MapLibre usage gives Oracle control over sources, layers, clustering, viewport behavior, and interactions.


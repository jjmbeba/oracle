# Keyed Map Tile Provider

Oracle will use a keyed map tile provider, with MapTiler as the first candidate, rather than depending directly on public OpenStreetMap tiles. Oracle is map-first, so tile availability, usage terms, styling, and predictable demo behavior matter from the MVP.


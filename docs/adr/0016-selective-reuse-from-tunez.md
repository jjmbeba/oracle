# Selective Reuse from Tunez

Oracle will use Tunez as a reference implementation for Better Auth anonymous sessions and project ergonomics, but it will not copy Tunez conventions wholesale. Oracle has distinct boundaries around maps, provider ingestion, worker scheduling, geospatial data, and region snapshots, so reuse should be intentional and fit those needs.


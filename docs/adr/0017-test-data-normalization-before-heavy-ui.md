# Test Data Normalization Before Heavy UI

Oracle will prioritize tests for provider adapters, scoring rules, and API contracts before investing heavily in UI tests. The highest MVP risk is whether public provider data is normalized, deduplicated, and scored correctly; map dashboard UI tests can expand after those data paths are stable.


# Zod for Validation and Normalization

Oracle will use Zod for validation and normalization schemas. Zod has strong TypeScript familiarity and ecosystem support around Hono and OpenAPI generation, which matters more for Oracle than minimizing validation library weight.


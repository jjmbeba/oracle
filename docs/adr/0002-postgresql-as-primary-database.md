# PostgreSQL as Primary Database

Oracle will use PostgreSQL as its primary database from the MVP. This adds more setup than SQLite-style options, but it better fits anonymous sessions, watched regions, signal snapshots, background ingestion state, and future geospatial querying.

